openapi: 3.1.0
info:
  title: No Tags Service Classification Test
  version: 1.0.0
  description: Test no-tags default service classification

paths:
  /health:
    get:
      operationId: health
      responses:
        '200':
          description: Success
          content:
            text/plain:
              schema:
                type: string

  /version:
    get:
      operationId: version
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: string

  /ping:
    get:
      operationId: ping
      responses:
        '200':
          description: Success
          content:
            text/plain:
              schema:
                type: string