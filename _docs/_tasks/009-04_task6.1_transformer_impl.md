# Task 6.1: OpenAPITransformer - 基本構造 TDD実装

## 🎯 実装目標

OpenAPIからIRへの変換機能を、t-wada推奨のTDD手法（Red-Green-Refactor）で実装します。

## 📝 実装アプローチ

### 設計方針

- **関数ベース**: Tree-shaking効率化のためクラスを使用しない
- **モジュール分離**: 単一責任原則に基づく機能分離
- **純粋関数**: 副作用のない関数型プログラミング

## 📂 最終的なファイル構成

```
packages/core/
├── tests/
│   └── transformer/
│       ├── basic.test.ts                      # 統合テスト（1テスト）
│       └── extractors/
│           └── metadata-extractor.test.ts      # 単体テスト（5テスト）
├── src/
│   └── transformer/
│       ├── index.ts                            # エクスポート用
│       ├── openapi-transformer.ts              # transform関数
│       └── extractors/
│           ├── index.ts                        # エクスポート用
│           └── metadata-extractor.ts           # extractMetadata関数
```

## 🔧 実装詳細

### メイン変換関数

```typescript
// src/transformer/openapi-transformer.ts
import type { OpenAPIDocument, XcgenIR } from "../types";
import { extractMetadata } from "./extractors";

/**
 * Transform OpenAPI document to XcgenIR
 * @param doc - OpenAPI document to transform
 * @returns XcgenIR representation
 */
export function transform(doc: OpenAPIDocument): XcgenIR {
  return {
    metadata: extractMetadata(doc),
    models: [],
    enums: [],
    unions: [],
    services: [],
    servers: [],
  };
}
```

### メタデータ抽出関数

```typescript
// src/transformer/extractors/metadata-extractor.ts
export function extractMetadata(doc: OpenAPIDocument): IRMetadata {
  return {
    title: doc.info.title,
    version: doc.info.version,
    description: doc.info.description,
    openApiVersion: "openapi" in doc ? doc.openapi : "3.0.0",
    contact: extractContact(doc),
    license: extractLicense(doc),
  };
}

function extractContact(doc: OpenAPIDocument): IRContact | undefined {
  // 実装
}

function extractLicense(doc: OpenAPIDocument): IRLicense | undefined {
  // 実装
}
```

## 📊 TDD実装フロー

### Phase 1: 基本構造の実装 ✅

#### Step 1.1: 初期実装

- [x] REDフェーズ: テストファイル作成
- [x] GREENフェーズ: 最小限の実装
- [x] REFACTORフェーズ: 型定義追加

#### Step 1.2: メタデータ抽出

- [x] REDフェーズ: メタデータテスト作成
- [x] GREENフェーズ: extractMetadata実装
- [x] REFACTORフェーズ: 関数分離

### Phase 2: リファクタリング ✅

#### Step 2.1: extractorsディレクトリへの分離

- [x] MetadataExtractorクラス作成（初期）
- [x] クラスから関数への変換
- [x] 単体テストの追加

#### Step 2.2: 関数ベースへの移行

- [x] OpenAPITransformerクラスを関数化
- [x] 関数名の簡素化（transform）
- [x] Tree-shaking対応

### Phase 3: テストの最適化 ✅

#### Step 3.1: テストの統合

- [x] 重複するテストケースの削除
- [x] 包括的な統合テストに変更
- [x] テスト数: 7 → 6へ最適化

## 📈 テスト実行結果

```bash
# 最終的なテスト実行結果
pnpm test tests/transformer/

✓ tests/transformer/extractors/metadata-extractor.test.ts (5 tests) 1ms
  ✓ extractMetadata
    ✓ should extract basic metadata
    ✓ should extract metadata with description
    ✓ should extract contact information
    ✓ should extract license information
    ✓ should handle missing optional fields

✓ tests/transformer/basic.test.ts (1 test) 6ms
  ✓ transform
    ✓ should transform OpenAPI document with complete structure

Test Files  2 passed (2)
     Tests  6 passed (6)
```

## ✅ 達成した改善点

### 1. コード品質

- **関数型プログラミング**: 純粋関数による実装
- **モジュール化**: 機能ごとに適切に分離
- **型安全性**: TypeScriptの型システムを活用

### 2. パフォーマンス

- **Tree-shaking対応**: 未使用コードの自動除外
- **バンドルサイズ削減**: クラスのオーバーヘッド削除
- **実行効率**: 関数呼び出しの最適化

### 3. 保守性

- **単一責任原則**: 各関数が1つの責任のみ
- **テスタビリティ**: 各関数を独立してテスト可能
- **可読性**: シンプルで理解しやすいコード

## 🚀 今後の実装予定

### 次のExtractor実装

- [ ] ServerExtractor: サーバー情報の抽出
- [ ] ModelExtractor: データモデルの抽出
- [ ] EnumExtractor: 列挙型の抽出
- [ ] UnionExtractor: Union型の抽出
- [ ] ServiceExtractor: APIサービスの抽出

### 実装方針

- 全て関数ベースで実装
- 各extractorに単体テストを追加
- transform関数から呼び出し

## 📝 使用方法

```typescript
import { transform } from "@openapi-xcgen/core/transformer";
import { OpenAPIParser } from "@openapi-xcgen/core/parser";

// OpenAPIドキュメントをパース
const parser = new OpenAPIParser();
const doc = await parser.parse("./api.yaml");

// IRに変換
const ir = transform(doc);

// IRを使用してコード生成
// ...
```

## 🎉 Task 6.1 完了

OpenAPITransformerの基本構造が、TDD手法と関数型プログラミングにより実装完了しました。
全てのテストがGreenで、Tree-shaking対応の効率的な実装を達成しています。
